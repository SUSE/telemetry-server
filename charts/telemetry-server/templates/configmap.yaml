apiVersion: v1
kind: ConfigMap
metadata:
  name: telemetry-server-config
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
  pre-deploy-checks: |
    {{- .Files.Get "scripts/pre_deploy_checks.sh" | nindent 4 }}
  file-from-cfgmap: |
    {{- toYaml .Values.config | nindent 4 }}
