api:
  host: tsg
  port: 9999
dbs:
  telemetry:
    driver: postgres
    params: "postgres://postgres:telemetry@db:5432/telemetry"
  operational:
    driver: postgres
    params: "postgres://postgres:telemetry@db:5432/operational"
  staging:
    driver: postgres
    params: "postgres://postgres:telemetry@db:5432/staging"
logging:
  level: info
  location: stderr
  style: text
auth:
  secret: VGVzdGluZ1NlY3JldAo=
  duration: 1w
